<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="60dp"
              android:background="@drawable/shape_background_drawer_item"
              android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/li_navigation_item_iv_picture"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/margin_smallest"
            android:scaleType="fitCenter"
            tools:src="@mipmap/app_ic_item_disable"/>

    </LinearLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/li_navigation_item_txt_name"
            style="@style/TextView"
            android:layout_marginRight="@dimen/margin_normal"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/gray"
            tools:text="@string/text_drawer"/>

    </LinearLayout>

</LinearLayout>